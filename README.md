# ParkingApp

Driveway Hourly Parking MVP monorepo scaffold.

## Workspace
- `apps/mobile` - React Native client (initial TypeScript scaffold)
- `services/api` - Node/TypeScript backend scaffold
- `packages/contracts` - Shared TypeScript contracts

## Quick start
```bash
pnpm install
pnpm -r build
```
