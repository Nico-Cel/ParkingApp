{
  "name": "@parkingapp/contracts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"contracts lint pending\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"contracts tests pending\""
  }
}
